import React from "react"
import Logo from "./image"

const Header = () => (
  <div 
    style={{
      margin: `20px auto`,
      textAlign: `center`
    }}
  >
    <Logo/>
  </div>
)
export default Header
